%h2 Delivery
= render partial: "steps"
= form_tag wizard_path, method: :put do
  - @delivery_services.each do |ds|
    = radio_button_tag(:delivery, ds.id, @cart.delivery_service == ds)
    = ds.name
    %br/
  = submit_tag "Save", class: "btn btn-warning"